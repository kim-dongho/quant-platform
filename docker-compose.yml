services:
  # 1. 시계열 데이터베이스 (PostgreSQL + TimescaleDB)
  db:
    image: timescale/timescaledb:latest-pg14
    container_name: quant-db
    restart: always
    # parallel hash/sort(/dev/shm) 기본 64MB 한계로 500+ 심볼 DISTINCT ON 쿼리가 터지는 걸 방지
    shm_size: 1gb
    # - max_locks_per_transaction: 하이퍼테이블 다종목 스캔 시 락 확보 (1m candle 까지 합쳐 chunk ~6700개 환경 고려)
    # - work_mem: DISTINCT ON / sort 가 디스크 spill 없이 메모리에서 처리되도록 상향
    # - shared_buffers: 반복 조회되는 factors/market_data 캐시 확대
    command: >-
      postgres
      -c max_locks_per_transaction=8192
      -c work_mem=64MB
      -c shared_buffers=512MB
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: quant
    ports:
      - "5432:5432"
    volumes:
      - ./infra/postgres/data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user -d quant"]
      interval: 5s
      timeout: 5s
      retries: 5

  # 2. 백엔드 API (Go)
  api:
    build:
      context: ./apps/server
      dockerfile: Dockerfile
    container_name: quant-api
    ports:
      - "8080:8080"
    environment:
      DB_HOST: "db"
      DB_USER: "user"
      DB_PASSWORD: "password"
      DB_NAME: "quant"
      DB_PORT: "5432"
      DB_DSN: "host=db user=user password=password dbname=quant port=5432 sslmode=disable"
    depends_on:
      db:
        condition: service_healthy

  # 3. 퀀트 엔진 (Python Worker)
  engine:
    build:
      context: ./apps/engine
      dockerfile: Dockerfile
    container_name: quant-engine
    # 개발용 volume mount: 호스트 src/ 변경을 컨테이너에서 즉시 반영 (재빌드 불필요)
    # 패키지 의존성(requirements.txt) 추가 시에만 docker-compose build engine
    volumes:
      - ./apps/engine/src:/app/src
      # KIS 토큰 파일 영속화 — 컨테이너 재시작 시에도 토큰 재사용 (.gitignore 됨).
      - ./apps/engine/data:/app/data
    # 개발 편의 — Swagger UI(/docs) / OpenAPI 스펙(/openapi.json) 호스트 접근.
    # 프론트는 항상 Go API(:8080)를 거치므로 운영에는 불필요.
    ports:
      - "8000:8000"
    environment:
      DB_DSN: "postgresql://user:password@db:5432/quant"
      # 한국투자증권(KIS) Open API — .env에서 주입
      KIS_APP_KEY: ${KIS_APP_KEY:-}
      KIS_APP_SECRET: ${KIS_APP_SECRET:-}
      KIS_ACCOUNT_NUMBER: ${KIS_ACCOUNT_NUMBER:-}
      KIS_ACCOUNT_PRODUCT_CODE: ${KIS_ACCOUNT_PRODUCT_CODE:-01}
      KIS_MODE: ${KIS_MODE:-paper}
      # Slack 알림 (선택) — 비워두면 알림 비활성
      SLACK_WEBHOOK_URL: ${SLACK_WEBHOOK_URL:-}
    depends_on:
      db:
        condition: service_healthy

  # # 4. 프론트엔드 (Next.js)
  # web:
  #   build:
  #     context: ./apps/web
  #     dockerfile: Dockerfile
  #   container_name: quant-web
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     # 브라우저(클라이언트)는 로컬호스트로 접속
  #     NEXT_PUBLIC_API_URL: "http://localhost:8080"
