{
  "apps/web/**/*.{ts,tsx,js,jsx}": [
    "pnpm --filter web exec prettier --write"
  ],
  "apps/web/**/*.{json,md,css}": [
    "pnpm --filter web exec prettier --write"
  ],
  "apps/server/**/*.go": [
    "gofmt -w"
  ],
  "apps/engine/**/*.py": [
    "bash -c 'command -v ruff >/dev/null 2>&1 && ruff check --fix \"$@\" && ruff format \"$@\" || echo \"(ruff 미설치 — skip)\"' --"
  ]
}
