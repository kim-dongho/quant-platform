name: Deploy to VPS

on:
  push:
    branches: [dev]

concurrency:
  group: deploy-vps
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: [self-hosted, vps]
    steps:
      - name: Pull and rebuild on VPS
        run: /root/quant-platform/scripts/deploy.sh
